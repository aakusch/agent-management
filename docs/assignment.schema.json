{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://relay.local/schemas/assignment.schema.json",
  "title": "Relay driver assignment",
  "type": "object",
  "required": ["kind", "schemaVersion", "assignment", "workflow", "components", "driver"],
  "properties": {
    "kind": { "const": "relay.assignment" },
    "schemaVersion": { "const": "1.0" },
    "assignment": {
      "type": "object",
      "required": ["id", "title", "task", "createdAt"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "title": { "type": "string", "minLength": 1 },
        "task": { "type": "string", "minLength": 1 },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "additionalProperties": false
    },
    "workflow": { "$ref": "workflow.schema.json" },
    "components": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "name", "description", "kind", "version", "inputs", "outputs", "instruction"],
        "properties": {
          "id": { "type": "string" },
          "name": { "type": "string" },
          "description": { "type": "string" },
          "kind": { "enum": ["agent", "judge", "router", "human", "tool", "workflow"] },
          "icon": { "type": "string" },
          "color": { "type": "string" },
          "version": { "type": "string" },
          "tags": { "type": "array", "items": { "type": "string" } },
          "inputs": { "type": "array", "items": { "type": "string" } },
          "outputs": { "type": "array", "items": { "type": "string" } },
          "instruction": { "type": "string" },
          "defaults": { "type": "object", "additionalProperties": { "type": "string" } },
          "workflowId": { "type": "string" }
        },
        "additionalProperties": false
      }
    },
    "modules": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "description", "version", "inputs", "outputs", "nodes", "edges", "entryNodeIds", "exitNodeIds"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "name": { "type": "string" },
          "description": { "type": "string" },
          "version": { "type": "string" },
          "icon": { "type": "string" },
          "color": { "type": "string" },
          "tags": { "type": "array", "items": { "type": "string" } },
          "inputs": { "type": "array", "items": { "type": "string" } },
          "outputs": { "type": "array", "items": { "type": "string" } },
          "source": { "enum": ["built-in", "user"] },
          "nodes": { "type": "array", "items": { "type": "object" } },
          "edges": { "type": "array", "items": { "type": "object" } },
          "entryNodeIds": { "type": "array", "items": { "type": "string" } },
          "exitNodeIds": { "type": "array", "items": { "type": "string" } },
          "createdAt": { "type": "string", "format": "date-time" }
        },
        "additionalProperties": false
      }
    },
    "driver": {
      "type": "object",
      "required": ["protocol", "role", "concurrency", "stateDirectory", "eventLog", "artifactDirectory", "checkpointAfterEachNode", "specification", "defaultModel", "defaultEffort", "tools", "stopConditions", "permissions"],
      "properties": {
        "protocol": { "const": "relay-driver-v1" },
        "role": { "type": "string" },
        "concurrency": { "type": "integer", "minimum": 1 },
        "stateDirectory": { "type": "string" },
        "eventLog": { "type": "string" },
        "artifactDirectory": { "type": "string" },
        "checkpointAfterEachNode": { "type": "boolean" },
        "specification": {
          "type": "object",
          "required": ["enabled", "componentId", "mode", "artifact", "maySelectOptionalModules", "mayBindProjectCommands", "mayConfigureNodes", "mayRemoveRequiredModules", "mayWidenPermissions"],
          "properties": {
            "enabled": { "type": "boolean" },
            "componentId": { "const": "workflow-specifier" },
            "mode": { "enum": ["guided", "exact"] },
            "artifact": { "const": "run-spec.json" },
            "maySelectOptionalModules": { "type": "boolean" },
            "mayBindProjectCommands": { "type": "boolean" },
            "mayConfigureNodes": { "type": "boolean" },
            "mayRemoveRequiredModules": { "const": false },
            "mayWidenPermissions": { "const": false }
          },
          "additionalProperties": false
        },
        "defaultModel": { "type": "string", "minLength": 1 },
        "defaultEffort": { "enum": ["low", "medium", "high", "xhigh"] },
        "tools": { "type": "array", "items": { "enum": ["filesystem", "terminal", "git", "browser", "web"] }, "uniqueItems": true },
        "stopConditions": {
          "type": "object",
          "required": ["maxTotalSteps", "maxDurationMinutes", "stopOnNoProgress", "requireHumanOnExhaustion"],
          "properties": {
            "maxTotalSteps": { "type": "integer", "minimum": 1 },
            "maxDurationMinutes": { "type": "integer", "minimum": 1 },
            "maxCostUsd": { "type": "number", "minimum": 0 },
            "stopOnNoProgress": { "type": "integer", "minimum": 1 },
            "requireHumanOnExhaustion": { "type": "boolean" }
          },
          "additionalProperties": false
        },
        "permissions": {
          "type": "object",
          "required": ["spawnAgents", "shell", "network", "publish"],
          "properties": {
            "spawnAgents": { "type": "boolean" },
            "shell": { "enum": ["project", "read-only", "none"] },
            "network": { "enum": ["ask", "allow", "deny"] },
            "publish": { "enum": ["ask", "allow", "deny"] }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
