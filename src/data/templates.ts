import type { WorkflowTemplate } from '../types/catalog'

/** Templates come from the repository's `templates/` directory or an import — none are bundled. */
export const builtInTemplates: WorkflowTemplate[] = []
